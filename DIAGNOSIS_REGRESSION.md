# AI冷蔵庫診断 回帰テスト

更新日: 2026-08-28

## 自動テスト
`test-diagnosis.mjs` を GitHub Actions `Catalog validation` に組み込み、カタログ検証後に診断ロジックの回帰テストを実行する。

## 固定したシナリオ
1. 1人暮らし: 100〜299Lのhard rangeから外れる大型機を通常候補へ出さない
2. 2人暮らし: 通常候補は設定予算＋40,000円以内
3. 自動製氷必須: 候補は `autoIce:true` のみ
4. スマホ連携必須: 候補は `smartphone:true` のみ
5. 4人暮らし: 500L未満を候補に出さない
6. 横幅制限: 設定幅を超える機種を候補に出さない
7. 壁側約100°ルール: 左右の片開きをhard blockしない
8. 通常3候補: 同一シリーズキーを重複させない
9. 機能重視枠: 通常候補と同一シリーズを重複させない
10. `GR-Y600FK-EW`: ヨドバシ売り切り在庫として診断filterを通過する
11. `SJ-X373P-N`: ヨドバシ取扱終了判定後は商品poolへ残さない

## 回帰テストで発見・修正した不具合
### GR-Y600FKのstatus不整合
batch18で `status:"発売中（売り切り在庫）"` としていたため、`hardFilter` の `status === "発売中"` に一致せず、追加済みでも診断候補から除外されていた。

修正:
- `status:"発売中"`
- `lifecycle:"売り切り在庫"`

validatorにも、ロード対象商品の `status` は必ず `発売中` とする契約を追加した。生産終了後の売り切りなどの属性は `lifecycle` 等の別フィールドで保持する。

### スタッフ向け説明文の旧ロジック残存
画面内のスタッフ向け内部説明に、旧版の「戦略メーカー優先 / AQUA抑制」の説明が残っていた。

修正:
- `v89-ui-guidance.js` を追加
- 現行v8.9の「メーカー一律加減点なし、同等条件時のみ戦略メーカーをタイブレーク」に説明を統一

## 検証結果
2026-08-28 GitHub Actionsで:
- `Catalog validation: PASS`
- `Diagnosis regression: PASS`
- カタログ151機種
- カタログWarnings 0
- 潜在的な色違い / ベース型番重複 0

## 本番反映について
GitHub側のコードとCIは準備済み。ただしVercelはbuild rate limit中のため、batch15以降を含む最新mainはまだ本番配信確認できていない。

Vercel制限解除後に、本番URLで以下を必ず確認する。
- batch15〜19のHTTP 200
- `v89-ui-guidance.js` のHTTP 200
- 代表シナリオのブラウザ実診断
- コンソールエラーなし
